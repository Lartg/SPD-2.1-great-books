import classNames from "classnames";

export default function Feed() {
  return (
    <div className={classNames("box w-full")}>
      <h1 className={classNames("px-2 pb-2 bg-neutral-900 border-b-2 divider")}>Feed</h1>
    </div>
  );
}
